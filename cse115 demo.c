/*
// getting the whole name as input with gets cause scan only counts the first part..not after space...so going with *gets* for space

#include<stdio.h>
int main()
{
    char name[10]
    printf("Please enter your name: ");
    gets(name);

    printf("My name is %s", name);  //used %s because when we input letters with spaces it becomes a string.
    return 0;
}
*/
